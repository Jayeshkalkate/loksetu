services:
  - type: web
    name: loksetu

    env: python

    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate

    startCommand: |
      gunicorn loksetu.wsgi:application